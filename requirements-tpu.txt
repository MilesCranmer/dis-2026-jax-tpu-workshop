# Tested local CPU validation versions on Python 3.11.
# On Cloud TPU VM, install with: pip install -r requirements-tpu.txt
jax[tpu]
flax
optax
tiktoken
numpy
matplotlib
requests
nbformat==5.10.4
jupyterlab==4.5.9
# Optional checkpoint cell support; safe to omit if that cell is not used.
orbax-checkpoint==0.11.30
